name: schema-parity
on:
  pull_request:
    paths: ["content/**/index.mdx"]
jobs:
  parity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/check_schema_parity.mjs
